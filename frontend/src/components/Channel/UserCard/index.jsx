import React from "react";
import { Grid } from "@material-ui/core";

const UserCard = (props) => {
  const { channel } = props;
  console.log(channel);
  return <Grid>채널의 </Grid>;
};

export default UserCard;
